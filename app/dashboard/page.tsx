// Dashboard page
// The layout handles parallel routes (@default, @activity, @stats)
// The @default slot provides the children prop to the layout
export default function DashboardPage() {
  // Content is provided by @default/page.tsx via parallel routes
  return null;
}
